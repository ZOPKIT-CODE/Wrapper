/** @type {import('tailwindcss').Config} */
/**
 * Tailwind v4: tokens, animations, and shadows live in src/index.css (@theme inline + :root).
 * This file is kept for tooling compatibility; avoid adding theme extensions here.
 */
export default {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
  darkMode: 'class',
}
